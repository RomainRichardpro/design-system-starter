import { Meta } from '@storybook/blocks';
import { SpacingTokens } from '../../components/TokenStatus';

<Meta title="Fondations/Espacements" />

# Espacements

Les tokens d'espacement définissent les marges, paddings et rayons de bordure.
La largeur de chaque barre est proportionnelle à la valeur du token.

<SpacingTokens />
